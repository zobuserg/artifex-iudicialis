# Amanuensis — dependencias de ejecución
# Instalar desde la raíz del repo:  pip install -r app/requirements.txt

PyQt6>=6.7.0
pdfplumber>=0.11.0
pymupdf>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0
python-docx>=1.0.0
reportlab>=4.0.0
# Cliente HTTP para `app/core/claude_worker.py` (Ollama local); opcional si solo usas prompts en Cursor
ollama>=0.4.0
# Generación con API de Anthropic (`ClaudeWorker`)
anthropic>=0.40.0
